import { Button, SafeAreaView, StyleSheet, Text, View } from 'react-native'
import React from 'react'


const Home = () => {
    return (
        <SafeAreaView>
            <Button title='fgdgjfgf' onPress={() => { }} />
        </SafeAreaView>
    )
}

export default Home

const styles = StyleSheet.create({})